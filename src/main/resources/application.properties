spring.application.name=couzl

# 기본 활성 프로파일 (운영 배포 시 -Dspring.profiles.active=prod 으로 override)
spring.profiles.active=dev

# JSP View Resolver
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp
